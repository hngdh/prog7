package common.enums;

/**
 * The {@code CommandFormats} enum represents the different types of command formats based on their
 * arguments. It defines whether a command requires no arguments, a numerical argument, or a string
 * argument.
 */
public enum CommandFormats {
  WITHOUT_ARG,
  WITH_NUMERAL_ARG,
  WITH_STRING_ARG
}
